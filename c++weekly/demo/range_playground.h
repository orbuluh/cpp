#include <ranges>
#include <vector>

// llvm Clang 15.0.1 not supported yet...

namespace range_playground {

void demo() {
    //std::vector<int> vec{1, 2, 3, 4, 5, 6};
    //auto result = std::ranges::all_of(vec, [](const int i) { return i % 2; });
    //std::cout << result << '\n';
}
}  // namespace range_playground